# AI Employee — Environment Variables (from openclaw-2)
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ── Security (REQUIRED) ────────────────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=

# ── AI API Keys (Optional — only if using external AI providers) ───────────────
# Leave empty to use the local Ollama model instead.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ── Per-Agent Model Routing (Optional — upgrade specific agents) ───────────────
# Sales & persuasion agents use GPT-4o by default for better copy quality.
# Set to "gpt-4o-mini" to reduce costs while still using OpenAI.
# OPENAI_MODEL_SALES=gpt-4o
# OPENAI_SALES_MODEL=gpt-4o       # legacy alias still supported
# OPENAI_MODEL_CREATIVE=gpt-4o
# CLAUDE_MODEL=claude-opus-4-6

# ── Ollama (Local AI — free & private) ────────────────────────────────────────
# Install from https://ollama.com and run: ollama pull llama3.2
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=llama3.2
# OLLAMA_TIMEOUT=60
# LOCAL_AI_FIRST=1                       # always try Ollama before cloud providers (default: 1)
# OLLAMA_BATCH_MAX_WORKERS=4             # parallel workers for query_ai_batch() (default: 4)

# ── Agent Memory (Optional — persistent per-lead conversation history) ─────────
# MEMORY_MAX_CONVERSATION=50
# MEMORY_MAX_FACTS=100

# ── Feedback Loop (Optional — automatic prompt optimisation) ──────────────────
# FEEDBACK_POLL_INTERVAL=30
# FEEDBACK_TOP_N=5

# ── Task Orchestrator (Optional) ──────────────────────────────────────────────
# TASK_ORCHESTRATOR_MAX_PARALLEL=10    # run up to 10 agents concurrently
# TASK_ORCHESTRATOR_PEER_REVIEW=true   # agents review each other's outputs


# ── Dashboard Ports (Optional — defaults shown) ────────────────────────────────
# PROBLEM_SOLVER_UI_PORT=8787
# DASHBOARD_PORT=3000

# ── Logging (Optional) ────────────────────────────────────────────────────────
# Orchestrator and dashboard default to INFO; all other bots default to WARNING.
# Set LOG_LEVEL=INFO on a specific bot's process to get verbose output from it.
# LOG_LEVEL=WARNING

# ── Rate Limiting (Optional — defaults shown) ──────────────────────────────────
# RATE_LIMIT_PER_MINUTE=60
# MAX_SESSIONS_PER_USER=3

# ── Discord Bot (Optional — admin control panel) ──────────────────────────────
# Create a bot at https://discord.com/developers/applications, enable
# "Message Content Intent", invite it to your server, then paste the token below.
# DISCORD_BOT_TOKEN=
# DISCORD_COMMAND_PREFIX=!

# ── WhatsApp Inbound Webhook (Optional — required for reply detection) ─────────
# Start runtime/bots/whatsapp-webhook/webhook_server.py and point your Twilio
# WhatsApp number's "A MESSAGE COMES IN" webhook URL to:
#   http://<your-server>:8790/webhook/whatsapp
# WHATSAPP_WEBHOOK_PORT=8790
# WHATSAPP_WEBHOOK_HOST=0.0.0.0

# ── WhatsApp Integration (Optional — choose one provider) ─────────────────────
# Option A: Twilio WhatsApp (recommended)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_FROM=whatsapp:+14155238886   # your Twilio WhatsApp number
# Option B: Meta WhatsApp Cloud API
# WHATSAPP_TOKEN=
# WHATSAPP_PHONE_ID=
# Set to "true" to log without actually sending (for testing)
# WHATSAPP_DRY_RUN=false

# ── Email Integration (Optional — choose one provider) ────────────────────────
# Option A: SMTP (Gmail, Outlook, Mailgun SMTP, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@example.com
# SMTP_PASS=your-app-password
# SMTP_FROM=you@example.com
# SMTP_USE_TLS=true
# Option B: SendGrid (better deliverability for cold outreach)
# SENDGRID_API_KEY=
# SENDGRID_FROM=you@example.com
# Set to "true" to log without actually sending (for testing)
# EMAIL_DRY_RUN=false
# EMAIL_REPLY_TO=

# ── Additional API Keys (Optional) ────────────────────────────────────────────
# ALPHA_INSIDER_KEY=
# TAVILY_API_KEY=                       # Best AI web search quality
# SERP_API_KEY=                         # Google search via SerpAPI
# NEWS_API_KEY=
# TELEGRAM_BOT_TOKEN=
# DISCORD_WEBHOOK_URL=
# ELEVEN_LABS_KEY=

# ── SMTP Email (Optional — for email_sender tool) ─────────────────────────────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your_app_password
# SMTP_USE_SSL=false

# ── Twilio WhatsApp (Optional — for whatsapp tool) ────────────────────────────
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# ── Agent Memory (Optional — defaults shown) ──────────────────────────────────
# MEMORY_MAX_TURNS=50

# ── Feedback Loop (Optional — defaults shown) ─────────────────────────────────
# FEEDBACK_LOOP_POLL_INTERVAL=30
# FEEDBACK_MIN_SENDS=5
# FEEDBACK_LOW_REPLY_RATE=0.10
# FEEDBACK_MAX_EVENTS=1000

# ── Task Orchestrator (Optional — defaults shown) ─────────────────────────────
# TASK_ORCHESTRATOR_MAX_PARALLEL=10
# TASK_ORCHESTRATOR_PEER_REVIEW=true
# TASK_ORCHESTRATOR_TIMEOUT=600
