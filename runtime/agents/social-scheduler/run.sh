#!/usr/bin/env bash
# social-scheduler — Social media scheduling and automation
# Runs as an embedded service within the AI Employee server.
echo "social-scheduler is running as an embedded service in the AI Employee server."
