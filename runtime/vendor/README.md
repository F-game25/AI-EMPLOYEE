# Runtime Vendor Area

Patched open-source code that is built into AETERNUS NEXUS belongs under:

`runtime/vendor/aeternus_vendor/<project>`

Every vendored project must include its upstream source, version, license, checksum and local patch notes. Do not copy third-party source into core runtime paths under its original import name.
