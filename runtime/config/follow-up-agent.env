# follow-up-agent configuration
# Edit these values as needed

# Poll interval (seconds)
FOLLOWUP_POLL_INTERVAL=5

# Hours to wait between follow-up attempts
FOLLOWUP_WAIT_HOURS=48

# Maximum follow-up attempts before marking a lead as lost (2-5 recommended)
FOLLOWUP_MAX_ATTEMPTS=5
